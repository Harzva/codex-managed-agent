请按以下计划执行。先阅读入口文档，然后按顺序逐个实现子任务。

## 入口文档（必读）

.claude/plans/2026-04-27-active-plan.md

该文件包含：
- 两个专题的执行顺序（Team Runtime Visualization → Codex Memory Manager）
- 7 个子任务的列表和对应文档路径
- 现有代码上下文（必须了解的文件清单）
- 全局约束（单任务执行、测试命令、CSS 要求等）

## 执行规则

1. 一次只实现一个子任务
2. 每个子任务完成后运行：
     npm run validate:moa-dag
     npm run validate:role-plugins
3. 测试通过后再打开下一个子任务文档
4. 不要并行修改多个任务

## 现有代码速查

- Webview 渲染：src/webview-template.js（render() 分发、tab 系统、drawer）
- 样式：src/webview/styles.js（:root 深色变量 + body:is(...) 浅色覆盖）
- Host 消息：src/host/panel-view.js
- Codex Home：src/host/bundled-skills.js 中的 resolveCodexHome() → ~/.codex
- 主题修复：src/webview-template.js 中的 syncHtmlBackground()（不要破坏）

.claude/plans/2026-04-27-active-plan.md          ← 入口（你给的 prompt 指向这里）
    ├── 引用 → task-plans/20-product-surface/2026-04-27-team-runtime-visualization-roadmap.md
    │       ├── 引用 → ...task-1-operation-icons.md
    │       ├── 引用 → ...task-2-phase-badges.md
    │       └── 引用 → ...task-3-agent-status-bar.md
    │
    └── 引用 → task-plans/20-product-surface/2026-04-27-codex-memory-manager-roadmap.md
            ├── 引用 → ...memory-task-1-discovery.md
            ├── 引用 → ...memory-task-2-agents-editor.md
            ├── 引用 → ...memory-task-3-creator.md
            └── 引用 → ...memory-task-4-history.md