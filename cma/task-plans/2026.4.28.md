请实现 Codex Skill Manager Phase 1（Discovery & Browser）。

## 必读文档（按顺序）

1. 长期规划与架构决策：
   task-plans/00-roadmap/2026-04-27-codex-skill-manager-roadmap.md

2. 本期详细任务清单（含验收标准）：
   task-plans/10-agent-orchestration/2026-04-27-codex-skill-manager-phase-1-discovery.md

## 现有代码上下文

- 技能安装/扫描逻辑：src/host/bundled-skills.js
  - resolveCodexHome() → ~/.codex
  - resolveSkillsRoot() → ~/.codex/skills
  - bundledSkillState() / installBundledSkill() / listBundledSkillStates()
  - 辅助函数：readJson(), writeJson()

- Webview 渲染：src/webview-template.js
  - 现有 tabs: Overview, Board, Team, Insights, Loop
  - render() 函数根据 state.ui.currentView 分发
  - 已有 drawer/panel 系统可复用

- 样式：src/webview/styles.js
  - 深色变量在 :root，浅色覆盖在 body:is(.color-theme-light...) 选择器
  - 已有 .team-* 组件可作为风格参考

- VS Code 消息路由：src/host/panel-view.js 和 extension.js
  - 已有 acquireVsCodeApi + postMessage 模式

## 技能目录现状

bundled-skills/
├── codex-loop/          (.cma-skill-manifest.json + SKILL.md + scripts/ + references/)
└── team-reflective-loop/

用户安装后复制到 ~/.codex/skills/<name>/

## 执行范围

严格按 Phase 1 文档的 In Scope 实现，Out of Scope 的内容（GitHub import、uninstall、search、collections、marketplace）不要碰。

## 关键约束

1. 不要修改现有 Board/Team/Insights/Loop 页面的渲染逻辑
2. 不要修改 BUNDLED_SKILLS 数组定义
3. 不要修改 ~/.codex/skills/ 的目录结构约定
4. Markdown 渲染只需基础支持（h1-h3, list, code block, bold, link）
5. 优先复用现有 drawer 系统；如果没有则按 Phase 1 文档的 CSS 规范新建
6. 所有新增 CSS 必须包含浅色主题覆盖
7. 参考 VS Code Agent Skills TreeView 截图中的设计：Accordion 分区 + 计数徽章 + 内联操作

## 交付检查

实现完成后运行：
  npm run validate:moa-dag
  npm run validate:role-plugins
必须全部通过。

手动验证：
- Skills tab 可从顶部导航进入
- 2 个 bundled skill 可见
- 安装一个 skill 后出现在 Installed 区
- 点击行打开 Drawer，SKILL.md / Manifest / Files 三栏正常
- Install / Update / Open Folder 按钮上下文感知
- 浅色主题正常（html 背景已修复，不要引入新的黑色背景问题）