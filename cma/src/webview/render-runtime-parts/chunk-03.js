module.exports = "              promptFile: node.dataset.loopPromptFile || \"\",\n              threadId: node.dataset.loopThreadId || \"\",\n              intervalMinutes: node.dataset.loopIntervalMinutes || \"\",\n              maxTicks: node.dataset.loopMaxTicks || \"\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-restart-loop]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({ type: \"restartLoopDaemon\" });\n          });\n        });\n        document.querySelectorAll(\"[data-restart-loop-at]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            markPendingLoopAction(node.dataset.restartLoopAt || \"\", \"restart\");\n            render(state.payload);\n            vscode.postMessage({\n              type: \"restartLoopDaemonAt\",\n              stateDir: node.dataset.restartLoopAt || \"\",\n              workspace: node.dataset.loopWorkspace || \"\",\n              promptFile: node.dataset.loopPromptFile || \"\",\n              threadId: node.dataset.loopThreadId || \"\",\n              intervalMinutes: node.dataset.loopIntervalMinutes || \"\",\n              maxTicks: node.dataset.loopMaxTicks || \"\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-attach-loop-tmux]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({\n              type: \"attachLoopTmux\",\n              sessionName: node.dataset.attachLoopTmux || \"\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-tail-loop-log]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({\n              type: \"tailLoopLog\",\n              path: node.dataset.tailLoopLog || \"\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-open-external-url]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({\n              type: \"openExternalUrl\",\n              url: node.dataset.openExternalUrl || \"\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-install-codex-loop-skill]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({ type: \"installBundledCodexLoopSkill\" });\n          });\n        });\n        document.querySelectorAll(\"[data-bundled-skill]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({\n              type: \"installBundledSkill\",\n              skillName: node.dataset.bundledSkill || \"\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-open-codex-skills-folder]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({ type: \"openCodexSkillsFolder\" });\n          });\n        });\n        document.querySelectorAll(\"[data-sync-bundled-skills]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({ type: \"syncBundledSkills\" });\n          });\n        });\n        document.querySelectorAll(\"[data-provider-sync-action]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            const action = node.dataset.providerSyncAction || \"preview\";\n            vscode.postMessage({ type: action === \"apply\" ? \"applyProviderSync\" : \"previewProviderSync\" });\n          });\n        });\n        document.querySelectorAll(\"[data-generate-loop-rotation]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({\n              type: \"generateLoopRotationPrompt\",\n              stateDir: node.dataset.generateLoopRotation || \"\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-copy-loop-rotation]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({\n              type: \"copyLoopRotationPrompt\",\n              stateDir: node.dataset.copyLoopRotation || \"\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-copy-text]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            if (node.disabled) return;\n            vscode.postMessage({\n              type: \"copyText\",\n              text: node.dataset.copyText || \"\",\n              label: node.dataset.copyLabel || \"Copied\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-create-thread-from-play]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({ type: \"createThread\" });\n          });\n        });\n        document.querySelectorAll(\"[data-open-new-codex-thread]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({ type: \"createThread\" });\n          });\n        });\n        bindCopyThreadIdActions(document);\n        document.querySelectorAll(\"[data-open-repo-file]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({\n              type: \"openRepoFile\",\n              path: node.dataset.openRepoFile || \"\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-toggle-intervention]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            state.ui.interventionCollapsed = !state.ui.interventionCollapsed;\n            persistUi();\n            render(state.payload);\n          });\n        });\n        document.querySelectorAll(\"[data-clear-auto-loop]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({\n              type: \"clearAutoContinue\",\n              threadId: node.dataset.clearAutoLoop,\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-watch-auto]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            const threadId = node.dataset.watchAuto || \"\";\n            if (!threadId) return;\n            const promptValue = window.prompt(\"Auto-continue prompt\", node.dataset.watchPrompt || \"continue\");\n            if (promptValue === null) return;\n            const countValue = window.prompt(\"Auto-continue count\", node.dataset.watchCount || \"1\");\n            if (countValue === null) return;\n            vscode.postMessage({\n              type: \"setWatchAutoContinue\",\n              threadId,\n              prompt: String(promptValue || \"\").trim(),\n              count: Number(countValue),\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-watch-control]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            const threadId = node.dataset.watchControl || \"\";\n            const action = node.dataset.watchAction || \"\";\n            if (!threadId || !action) return;\n            vscode.postMessage({\n              type: \"watchControl\",\n              threadId,\n              action,\n              reason: \"webview\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-loop-preset]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            setLoopDraftCount(node.dataset.loopPreset || \"10\");\n            render(state.payload);\n          });\n        });\n        document.querySelectorAll(\"[data-loop-prompt-preset]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            setLoopDraftPrompt(node.dataset.loopPromptPreset || \"continue\");\n            render(state.payload);\n          });\n        });\n        document.querySelectorAll(\"[data-loop-prompt-input]\").forEach((node) => {\n          node.addEventListener(\"input\", (event) => {\n            setLoopDraftPrompt(event.target.value || \"\");\n          });\n        });\n        document.querySelectorAll(\"[data-loop-count-input]\").forEach((node) => {\n          node.addEventListener(\"input\", (event) => {\n            setLoopDraftCount(event.target.value || \"\");\n          });\n        });\n        document.querySelectorAll(\"[data-loop-close]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            closeLoopPanel();\n          });\n        });\n        document.querySelectorAll(\"[data-loop-apply]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({\n              type: \"setAutoContinue\",\n              threadId: node.dataset.loopApply,\n              prompt: state.ui.loopDraftPrompt,\n              count: Number(state.ui.loopDraftCount),\n            });\n            closeLoopPanel();\n          });\n        });\n        document.querySelectorAll(\"[data-resize-card]\").forEach((node) => {\n          node.addEventListener(\"pointerdown\", (event) => {\n            beginRunningCardResize(node.dataset.resizeCard, node.dataset.resizeCorner || \"se\", event);\n          });\n        });\n        document.querySelectorAll(\"[data-running-card]\").forEach((node) => {\n          node.addEventListener(\"pointerdown\", (event) => {\n            beginBoardPointerDrag(node.dataset.runningCard, event);\n          });\n          node.addEventListener(\"lostpointercapture\", (event) => {\n            cancelBoardPointerDrag(event);\n          });\n        });\n        document.querySelectorAll(\"[data-edit-card-name]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            beginInlineCardLabelEdit(node.dataset.editCardName, node);\n          });\n          node.addEventListener(\"keydown\", (event) => {\n            if (event.key !== \"Enter\" && event.key !== \" \") return;\n            event.preventDefault();\n            event.stopPropagation();\n            beginInlineCardLabelEdit(node.dataset.editCardName, node);\n          });\n        });\n        document.querySelectorAll(\"[data-set-board-tab]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            setThreadBoardTab(node.dataset.setBoardTab, node.dataset.currentBoardTab || \"\");\n          });\n        });\n        document.querySelectorAll(\"[data-board-tab]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            setActiveBoardTab(node.dataset.boardTab || \"all\");\n          });\n        });\n        document.querySelectorAll(\"[data-create-board-tab]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            createBoardTab();\n          });\n        });\n        document.querySelectorAll(\"[data-team-action]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            const action = node.dataset.teamAction || \"\";\n            if (action === \"initialize\") {\n              vscode.postMessage({ type: \"initializeTeamSpace\" });\n              return;\n            }\n            if (action === \"open_brief\") {\n              vscode.postMessage({ type: \"openTeamBrief\" });\n              return;\n            }\n            if (action === \"mark_stale\") {\n              vscode.postMessage({ type: \"markStaleTeamTasks\" });\n              return;\n            }\n            if (action === \"new_workspace\") {\n              node.textContent = \"Creating...\";\n              node.disabled = true;\n              vscode.postMessage({ type: \"createTeamWorkspace\", template: \"blank\" });\n              return;\n            }\n            if (action === \"new_workspace_template\") {\n              node.textContent = \"Creating...\";\n              node.disabled = true;\n              vscode.postMessage({ type: \"createTeamWorkspace\", template: node.dataset.teamTemplate || \"blank\" });\n              return;\n            }\n            if (action === \"assign_selected\") {\n              const checkedIds = filteredThreads.filter((thread) => isSelected(thread.id)).map((thread) => thread.id);\n              const threadId = checkedIds[0] || state.selectedThreadId || \"\";\n              if (!threadId) return;\n              vscode.postMessage({ type: \"assignTeamTask\", threadId });\n            }\n          });\n        });\n        document.querySelectorAll(\"[data-team-thread-action]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            const threadId = node.dataset.teamThread || \"\";\n            const action = node.dataset.teamThreadAction || \"\";\n            const messageType = {\n              claim: \"claimTeamTask\",\n              heartbeat: \"heartbeatTeamTask\",\n              block: \"blockTeamTask\",\n              complete: \"completeTeamTask\",\n            }[action];\n            if (!messageType || !threadId) return;\n            vscode.postMessage({ type: messageType, threadId });\n          });\n        });\n        document.querySelectorAll(\"[data-team-agent-action]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            const agentId = node.dataset.teamAgent || \"\";\n            const action = node.dataset.teamAgentAction || \"\";\n            if (action === \"edit_role\" && agentId) {\n              vscode.postMessage({ type: \"updateTeamAgentRolePrompt\", agentId });\n            }\n          });\n        });\n        document.querySelectorAll(\"[data-agent-pill]\").forEach((pill) => {\n          pill.addEventListener(\"click\", () => {\n            const agentId = pill.dataset.agentPill;\n            const target = document.querySelector('[data-team-agent=\"' + CSS.escape(agentId) + '\"]');\n            if (target) {\n              target.scrollIntoView({ behavior: \"smooth\", block: \"center\" });\n              target.style.outline = \"2px solid var(--cyan)\";\n              setTimeout(() => { target.style.outline = \"\"; }, 1500);\n            }\n          });\n        });\n        document.querySelectorAll(\"[data-team-retry-task]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({\n              type: \"retryTeamTask\",\n              taskId: node.dataset.teamRetryTask || \"\",\n              mode: node.dataset.teamRetryMode || \"same\",\n            });\n          });\n        });\n        document.querySelectorAll(\"[data-team-restart-task]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({ type: \"restartTeamTask\", taskId: node.dataset.teamRestartTask || \"\" });\n          });\n        });\n        document.querySelectorAll(\"[data-team-delete-task]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            vscode.postMessage({ type: \"deleteTeamTask\", taskId: node.dataset.teamDeleteTask || \"\" });\n          });\n        });\n        document.querySelectorAll(\"[data-team-task-detail]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            state.ui.teamTaskDrawerId = node.dataset.teamTaskDetail || \"\";\n            state.ui.drawerOpen = true;\n            persistUi();\n            render(state.payload);\n          });\n        });\n        document.querySelectorAll(\"[data-team-task-page]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            state.ui.teamTaskPageId = node.dataset.teamTaskPage || \"\";\n            state.ui.currentView = \"team\";\n            persistUi();\n            render(state.payload);\n          });\n        });\n        document.querySelectorAll(\"[data-team-task-page-back]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            state.ui.teamTaskPageId = undefined;\n            persistUi();\n            render(state.payload);\n          });\n        });\n        document.querySelectorAll(\"[data-team-workspace-page]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            state.ui.teamWorkspacePageId = node.dataset.teamWorkspacePage || \"\";\n            state.ui.teamTaskPageId = undefined;\n            state.ui.currentView = \"team\";\n            persistUi();\n            render(state.payload);\n          });\n        });\n        document.querySelectorAll(\"[data-team-workspace-page-back]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            state.ui.teamWorkspacePageId = undefined;\n            state.ui.teamTaskPageId = undefined;\n            persistUi();\n            render(state.payload);\n          });\n        });\n        document.querySelectorAll(\"[data-team-run-workspace]\").forEach((node) => {\n          node.addEventListener(\"click\", (event) => {\n            event.stopPropagation();\n            const workspaceId = node.dataset.teamRunWorkspace || \"\";\n            node.textContent = \"Run success\";\n            node.disabled = true;\n            vscode.postMessage({ type: \"runTeamWorkspace\", workspaceId });\n";
