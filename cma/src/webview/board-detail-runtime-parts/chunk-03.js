module.exports = "                '<div class=\"running-card-copy\">' +\n                  renderInlineCardTitle(thread, \"running-card-title\", titleMax, \"Running agent\") +\n                  renderEditableCardName(thread, { className: \"running-card-card-name\", maxLength: size === \"l\" ? 72 : 54 }) +\n                  rootCue +\n                  '<div class=\"running-card-subtitle\">' + esc(subtitle) + '</div>' +\n                  lifecycleStrip +\n                  cardUsageMeta +\n                  '<div class=\"preview\">' + esc(preview) + '</div>' +\n                  conversationStats +\n                  '<div data-running-loop-card=\"' + esc(thread.id) + '\">' + loopStatusCard + '</div>' +\n                '</div>' +\n                '<div class=\"running-card-side\">' +\n                  '<div class=\"phase-panel\"><div class=\"phase-head\"><span class=\"phase-title\">' + renderThemeVisual(phaseArtFor(phase.label), \"phase-art\", visualPhaseLabel, \"phase\") + '<span class=\"phase-label\">Inferred activity: ' + esc(phase.label) + '</span></span><span class=\"meta-pill\" data-running-link-meta=\"' + esc(thread.id) + '\">' + esc(linkMeta.isFocused ? \"Focused\" : (linkMeta.isSidebar ? \"Sidebar\" : (linkMeta.isOpen ? \"Linked\" : \"Inferred\"))) + '</span></div><div class=\"phase-copy\">' + esc(phase.copy) + '</div></div>' +\n                  '<div class=\"progress-head\"><span class=\"progress-label\">' + esc(progress.label) + '</span><span class=\"progress-value\" data-running-progress-value=\"' + esc(thread.id) + '\">' + esc(progress.percent !== undefined ? (String(progress.percent) + \"%\") : factualStatusLabel(threadStateFacts(thread).process)) + '</span></div>' +\n                  '<div class=\"progress-track\"><div class=\"progress-bar\" style=\"width:' + esc(String(progress.percent !== undefined ? progress.percent : 18)) + '%\"></div></div>' +\n                  '<div class=\"running-card-note\">' + esc(progress.note) + '</div>' +\n                '</div>'\n              )\n            : (\n                renderInlineCardTitle(thread, \"running-card-title\", titleMax, \"Running agent\") +\n                renderEditableCardName(thread, { className: \"running-card-card-name\", maxLength: 42 }) +\n                rootCue +\n                '<div class=\"running-card-subtitle\">' + esc(subtitle) + '</div>' +\n                lifecycleStrip +\n                cardUsageMeta +\n                (showPreview ? '<div class=\"preview\">' + esc(preview) + '</div>' : '') +\n                conversationStats +\n                (showRichPhase\n                  ? '<div class=\"phase-panel\"><div class=\"phase-head\"><span class=\"phase-title\">' + renderThemeVisual(phaseArtFor(phase.label), \"phase-art\", visualPhaseLabel, \"phase\") + '<span class=\"phase-label\">Inferred activity: ' + esc(phase.label) + '</span></span><span class=\"meta-pill\" data-running-link-meta=\"' + esc(thread.id) + '\">' + esc(linkMeta.isFocused ? \"Focused\" : (linkMeta.isSidebar ? \"Sidebar\" : (linkMeta.isOpen ? \"Linked\" : \"Inferred\"))) + '</span></div><div class=\"phase-copy\">' + esc(phase.copy) + '</div></div>'\n                  : '') +\n                '<div data-running-loop-card=\"' + esc(thread.id) + '\">' + loopStatusCard + '</div>' +\n                (showProgress\n                  ? '<div class=\"progress-head\"><span class=\"progress-label\">' + esc(progress.label) + '</span><span class=\"progress-value\" data-running-progress-value=\"' + esc(thread.id) + '\">' + esc(progress.percent !== undefined ? (String(progress.percent) + \"%\") : factualStatusLabel(threadStateFacts(thread).process)) + '</span></div>' +\n                    '<div class=\"progress-track\"><div class=\"progress-bar\" style=\"width:' + esc(String(progress.percent !== undefined ? progress.percent : 18)) + '%\"></div></div>' +\n                    '<div class=\"running-card-note\">' + esc(progress.note) + '</div>'\n                  : '')\n              );\n          return '<article class=\"running-card size-' + esc(size) + (size === \"tiny\" ? ' fixed-tiny' : '') + (options.compact ? ' compact-card' : '') + runningClass + codexClass + pinnedClass + attachedClass + phaseClass + projectClass + dropClass + needsAttentionClass + '\" data-running-card=\"' + esc(thread.id) + '\" data-grid-col=\"' + esc(String((placement && placement.col) || 1)) + '\" data-grid-row=\"' + esc(String((placement && placement.row) || 1)) + '\" draggable=\"' + esc(draggable) + '\"' + cardStyle + '>' +\n            '<div class=\"running-card-topbar\"></div>' +\n            '<div class=\"running-card-bottom-line\"></div>' +\n            '<div class=\"drop-slot left\"></div>' +\n            '<div class=\"drop-slot right\"></div>' +\n            compactTitlebar +\n            boardCardTopActions +\n            '<div class=\"running-card-top\">' +\n              '<div class=\"running-card-control\">' +\n                '<div class=\"control-label left\">Status</div>' +\n                '<div class=\"running-card-badges\" data-running-status-badges=\"' + esc(thread.id) + '\">' +\n                  renderThreadFactBadges(thread) +\n                  boardBadge(thread) +\n                  quotaCardWarning +\n                  renderBoardTabPill(thread) +\n                  renderLoopManagedBadge(thread.id) +\n                  '<span data-running-pending-badge=\"' + esc(thread.id) + '\">' + renderPendingPromptBadge(thread.id) + '</span>' +\n                  linkBadge +\n                  (size === \"tiny\" || options.compact ? renderCopyableThreadId(thread.id, { maxLength: 10 }) : '') +\n                '</div>' +\n              '</div>' +\n            '</div>' +\n            '<div class=\"running-card-body\">' +\n              bodyInner +\n              ((isTiny || isCompactTiny) && isQuickComposerOpen\n                ? '<div class=\"tiny-composer\">' +\n                    '<div class=\"tiny-composer-row\">' +\n                      '<input class=\"loop-input\" data-compose-prompt-input=\"' + esc(thread.id) + '\" value=\"' + esc(quickPrompt) + '\" placeholder=\"Prompt\" />' +\n                      '<button class=\"chip\" data-send-thread-prompt=\"' + esc(thread.id) + '\" type=\"button\">Send</button>' +\n                      '<button class=\"chip\" data-close-composer=\"' + esc(thread.id) + '\" type=\"button\">×</button>' +\n                    '</div>' +\n                  '</div>'\n                : '') +\n              '<span data-running-loop-meta=\"' + esc(thread.id) + '\">' + renderRunningAutoLoopMeta(autoLoop) + '</span>' +\n              '<span data-running-pending-meta=\"' + esc(thread.id) + '\">' + renderPendingPromptMeta(thread.id) + '</span>' +\n              ((!options.compact && !isTiny && !isCompactTiny) ? renderMemoryShortcutRow() : '') +\n              (isLoopPanelOpen\n                ? '<div class=\"loop-panel\">' +\n                    '<div class=\"phase-head\"><span class=\"phase-label\">Auto loop</span><span class=\"meta-pill\">Resume when stopped</span></div>' +\n                    '<div class=\"loop-presets\">' +\n                      '<button class=\"chip\" data-loop-preset=\"10\" type=\"button\">10</button>' +\n                      '<button class=\"chip\" data-loop-preset=\"50\" type=\"button\">50</button>' +\n                      '<button class=\"chip\" data-loop-preset=\"100\" type=\"button\">100</button>' +\n                      '<button class=\"chip\" data-loop-prompt-preset=\"continue\" type=\"button\">continue</button>' +\n                      '<button class=\"chip\" data-loop-prompt-preset=\"go on\" type=\"button\">go on</button>' +\n                    '</div>' +\n                    '<div class=\"loop-mini-inputs\">' +\n                      '<input class=\"loop-input\" data-loop-prompt-input=\"' + esc(thread.id) + '\" value=\"' + esc(state.ui.loopDraftPrompt || \"continue\") + '\" placeholder=\"Prompt\" />' +\n                      '<input class=\"loop-input\" data-loop-count-input=\"' + esc(thread.id) + '\" value=\"' + esc(state.ui.loopDraftCount || \"10\") + '\" placeholder=\"Count\" />' +\n                      '<button class=\"chip\" data-loop-apply=\"' + esc(thread.id) + '\" type=\"button\">Apply</button>' +\n                    '</div>' +\n                    '<div class=\"loop-presets\">' +\n                      '<button class=\"chip\" data-loop-close=\"' + esc(thread.id) + '\" type=\"button\">Close</button>' +\n                      (autoLoop ? '<button class=\"chip danger-chip\" data-clear-auto-loop=\"' + esc(thread.id) + '\" type=\"button\">Stop current</button>' : '') +\n                    '</div>' +\n                  '</div>'\n                : '') +\n            '</div>' +\n            '<div class=\"running-card-footer\">' +\n              '<div class=\"running-card-control\">' +\n                '<div class=\"control-label left\">Actions</div>' +\n                '<div class=\"running-action-rail\">' +\n                  ((isTiny || isCompactTiny)\n                    ? (\n                        '<button class=\"tool-btn' + (isQuickComposerOpen ? ' primary' : '') + '\" data-open-composer=\"' + esc(thread.id) + '\" data-current-prompt=\"' + esc(quickPrompt) + '\" type=\"button\">' + renderToolIcon('prompt') + '<span>Prompt</span></button>' +\n                        '<button class=\"tool-btn loop' + (isLoopManagedThread(thread.id) ? ' attached' : '') + '\"' + (isLoopManagedThread(thread.id) ? ' disabled' : ' data-set-loop-managed=\"' + esc(thread.id) + '\"') + ' type=\"button\">' + renderToolIcon('board') + '<span>' + (isLoopManagedThread(thread.id) ? 'Loop Attached' : 'Attach Loop') + '</span></button>'\n                      )\n                    : (\n                        '<button class=\"tool-btn\" data-set-board-tab=\"' + esc(thread.id) + '\" data-current-board-tab=\"' + esc(boardTabFor(thread.id)) + '\" type=\"button\">' + renderToolIcon('board') + '<span>' + esc(boardTabFor(thread.id) ? boardTabFor(thread.id) : '+ Tab') + '</span></button>' +\n                        '<button class=\"tool-btn primary\" data-open-codex-editor=\"' + esc(thread.id) + '\" type=\"button\">' + renderToolIcon('open') + '<span>Editor</span></button>' +\n                        renderTerminalResumeButton(thread, { className: \"tool-btn terminal-resume\", label: \"Terminal\", icon: true }) +\n                        '<button class=\"tool-btn\" data-open-conversation=\"' + esc(thread.id) + '\" type=\"button\">' + renderToolIcon('chat') + '<span>Chat</span></button>' +\n                        renderGitActionMenu(thread, { className: \"tool-btn git-action\", pushClassName: \"tool-btn git-action git-push\", icon: true }) +\n                        '<button class=\"tool-btn' + (isQuickComposerOpen ? ' primary' : '') + '\" data-open-composer=\"' + esc(thread.id) + '\" data-current-prompt=\"' + esc(quickPrompt) + '\" type=\"button\">' + renderToolIcon('prompt') + '<span>Prompt</span></button>' +\n                        '<button class=\"tool-btn board' + (isBoardAttached(thread.id) ? ' attached' : '') + '\" data-board-attach=\"' + esc(thread.id) + '\" type=\"button\">' + renderToolIcon('board') + '<span>' + (isBoardAttached(thread.id) ? 'Unattach Board' : 'Attach Board') + '</span></button>' +\n                        '<button class=\"tool-btn loop' + (isLoopManagedThread(thread.id) ? ' attached' : '') + '\"' + (isLoopManagedThread(thread.id) ? ' disabled' : ' data-set-loop-managed=\"' + esc(thread.id) + '\"') + ' type=\"button\">' + renderToolIcon('board') + '<span>' + (isLoopManagedThread(thread.id) ? 'Loop Attached' : 'Attach Loop') + '</span></button>' +\n                        '<button class=\"tool-btn pin' + (isPinned(thread.id) ? ' pinned' : '') + '\" data-pin-thread=\"' + esc(thread.id) + '\" type=\"button\">' + renderToolIcon('pin', isPinned(thread.id)) + '<span>' + (isPinned(thread.id) ? 'Pinned' : 'Pin') + '</span></button>' +\n                        (autoLoop ? '<button class=\"tool-btn\" data-clear-auto-loop=\"' + esc(thread.id) + '\" type=\"button\">Stop Loop</button>' : '')\n                      )) +\n                '</div>' +\n              '</div>' +\n              ((isTiny || isCompactTiny) ? '' : (\n                '<div class=\"running-card-control\">' +\n                  '<div class=\"control-label\">Thread Id</div>' +\n                  renderCopyableThreadId(thread.id, { prefix: \"\", maxLength: 24, className: \"tool-id\" }) +\n                '</div>'\n              )) +\n            '</div>' +\n          '</article>';\n        }).join(\"\") || renderCuteEmpty(\"Board is empty\", \"Attach threads from the explorer below, or wait for a running agent to appear automatically.\", MEDIA.board);\n      }\n\n      function renderBoardCanvas(boardThreads, mode = \"status\") {\n        const groups = buildBoardColumnGroups(boardThreads, mode);\n        if (!groups.length) {\n          return renderCuteEmpty(\"Board is empty\", \"Attach threads from the explorer below, or wait for a running agent to appear automatically.\", MEDIA.board);\n        }\n        const sections = groups.map((group, index) => {\n          const accent = projectGroupAccent(group.key);\n          const boardId = boardProjectDomId(group.key, index);\n          const collapsed = mode === \"directory\" && isBoardProjectCollapsed(group.key);\n          return '<section class=\"board-project-section' + (collapsed ? ' collapsed' : '') + '\" style=\"--project-accent: ' + esc(accent) + '\" data-board-project=\"' + esc(group.key) + '\">' +\n            '<div class=\"board-project-head\">' +\n              '<div class=\"board-project-title\"><span class=\"board-project-dot\"></span><span title=\"' + esc(group.title || group.label || \"-\") + '\">' + esc(short(group.label || group.title || \"-\", 52)) + '</span></div>' +\n              '<span class=\"board-project-actions\"><button class=\"board-project-toggle\" data-board-project-toggle=\"' + esc(group.key) + '\" type=\"button\" aria-pressed=\"' + esc(collapsed ? \"true\" : \"false\") + '\">' + esc(collapsed ? \"Expand\" : \"Collapse\") + '</button><span class=\"board-project-count\">' + esc(String((group.threads || []).length)) + '</span></span>' +\n            '</div>' +\n            '<div id=\"' + esc(boardId) + '\" class=\"running-board-grid\" data-board-project-grid=\"' + esc(group.key) + '\">' +\n              renderRunningBoard(group.threads, { projectRootKey: group.key, groupKey: group.key, stacked: true }) +\n            '</div>' +\n          '</section>';\n        }).join(\"\");\n        if (mode !== \"directory\") return sections;\n        const nav = '<aside class=\"board-project-nav\" aria-label=\"Project directory navigation\">' +\n          '<div class=\"board-project-nav-head\"><span>Projects</span><span class=\"board-project-count\">' + esc(String(groups.length)) + '</span></div>' +\n          '<div class=\"board-project-nav-actions\">' +\n            '<button class=\"board-project-nav-btn\" data-board-project-expand-all=\"true\" type=\"button\">Expand all</button>' +\n            '<button class=\"board-project-nav-btn\" data-board-project-collapse-all=\"true\" type=\"button\">Collapse all</button>' +\n          '</div>' +\n          '<div class=\"board-project-nav-list\">' +\n            groups.map((group, index) => {\n              const boardId = boardProjectDomId(group.key, index);\n              return '<button class=\"board-project-nav-item\" data-board-project-jump=\"' + esc(boardId) + '\" data-board-project-key=\"' + esc(group.key) + '\" type=\"button\" title=\"' + esc(group.title || group.label || \"-\") + '\">' +\n                '<span class=\"board-project-nav-label\">' + esc(short(group.label || group.title || \"-\", 34)) + '</span>' +\n                '<span class=\"board-project-count\">' + esc(String((group.threads || []).length)) + '</span>' +\n              '</button>';\n            }).join(\"\") +\n          '</div>' +\n        '</aside>';\n        return nav + sections;\n      }\n\n      function buildDrawerDetailContext(payload) {\n        const detail = payload && payload.detail;\n        const dashboard = (payload && payload.dashboard) || { threads: [] };\n        const thread = detail && detail.thread ? detail.thread : {};\n        const summary = (dashboard.threads || []).find((item) => item.id === thread.id) || {};\n        const logs = detail && Array.isArray(detail.logs) ? detail.logs : [];\n        const history = Array.isArray(thread.history) ? thread.history : [];\n        const isArchived = Boolean(thread.archived || summary.archived);\n        const isSoftDeleted = Boolean(summary.soft_deleted || thread.soft_deleted);\n        const processText = summary.process && summary.process.summary ? summary.process.summary : \"No live process\";\n        const linkMeta = codexLinkMeta(thread.id, payload);\n        const linkLabel = linkMeta.isFocused ? \"Focused in Codex\" : (linkMeta.isSidebar ? \"Shown in Codex Sidebar\" : (linkMeta.isOpen ? \"Open in Codex\" : (linkMeta.pending ? \"Linking to Codex\" : \"Not linked\")));\n        const visibilityLabel = codexVisibilityLabel(Object.assign({}, summary, thread), payload);\n        const phase = inferCodexPhase(Object.assign({}, summary, thread));\n        const phaseClass = phaseClassFor(phase.label).trim();\n        const coordination = effectiveCoordinationTruth(Object.assign({}, summary, thread), payload);\n        const pendingDrawerAction = state.ui.pendingDrawerAction && state.ui.pendingDrawerAction.threadId === (thread.id || \"\")\n          ? state.ui.pendingDrawerAction\n          : undefined;\n        const confirmMeta = pendingDrawerAction ? getDrawerConfirmMeta(pendingDrawerAction.action) : undefined;\n        const actionNotice = payload && payload.actionNotice;\n        return {\n          detail,\n          dashboard,\n          thread,\n          summary,\n          logs,\n          history,\n          isArchived,\n          isSoftDeleted,\n          processText,\n          linkLabel,\n          visibilityLabel,\n          phase,\n          phaseClass,\n          coordination,\n          pendingDrawerAction,\n          confirmMeta,\n          actionNotice,\n        };\n      }\n\n      function findTeamTaskForDrawer(payload, taskId) {\n        const nextTaskId = String(taskId || \"\").trim();\n        const tasks = payload && payload.teamCoordination && Array.isArray(payload.teamCoordination.tasks)\n          ? payload.teamCoordination.tasks\n          : [];\n        return nextTaskId ? tasks.find((task) => task && task.task_id === nextTaskId) || null : null;\n      }\n\n      function teamTaskInputValue(task, type) {\n        return String((((task && task.inputs) || []).find((item) => item && item.type === type) || {}).value || \"\");\n      }\n\n      function renderTeamIdentifierBlock(task) {\n        const runtime = task && task.runtime && typeof task.runtime === \"object\" ? task.runtime : {};\n        const taskId = String((task && task.task_id) || \"\");\n        const owner = String((task && task.owner) || \"\");\n        const resolvedThreadId = String(runtime.thread_id || (!owner.startsWith(\"pending-team-worker-\") ? owner : \"\") || \"\");\n        const pid = runtime.pid ? String(runtime.pid) : \"\";\n        const rows = [\n          { label: \"Task ID\", value: taskId || \"-\" },\n          { label: \"Owner Agent ID\", value: owner || \"-\" },\n          { label: \"Resolved Thread ID\", value: resolvedThreadId || \"-\" },\n          { label: \"PID\", value: pid || \"-\" },\n        ];\n        return '<div class=\"team-id-list\">' +\n          rows.map((row) => (\n            '<div class=\"team-id-row\">' +\n              '<span class=\"team-id-label\">' + esc(row.label) + '</span>' +\n              '<span class=\"team-id-value\">' + esc(row.value) + '</span>' +\n            '</div>'\n          )).join(\"\") +\n        '</div>';\n      }\n\n      function describeTeamRetryState(runtime) {\n        const mode = String((runtime && runtime.retry_mode) || \"\").trim();\n        const state = String((runtime && runtime.state) || \"\").trim();\n        const modeLabel = mode === \"same\"\n          ? \"Retry same thread\"\n          : (mode === \"new\" ? \"Retry new worker\" : \"\");\n        const dispatchLabel = state === \"retrying\" ? \"Fresh retry dispatch\" : \"\";\n        return {\n          modeLabel,\n          dispatchLabel,\n          summary: [modeLabel, dispatchLabel].filter(Boolean).join(\" · \"),\n        };\n      }\n\n      function teamRunActionLabel(status) {\n        const nextStatus = String(status || \"\").toLowerCase();\n        if ([\"failed\", \"blocked\", \"error\", \"stale\"].includes(nextStatus)) return \"Retry\";\n        if ([\"queued\", \"draft\", \"ready\", \"pending\"].includes(nextStatus)) return \"Run\";\n        return \"Run Again\";\n      }\n\n      function teamTraceFiles(task) {\n        const source = task && task.trace_files && typeof task.trace_files === \"object\" ? task.trace_files : {};\n        function normalize(entry) {\n          const next = entry && typeof entry === \"object\" ? entry : {};\n          return {\n            path: String(next.path || \"\"),\n            exists: Boolean(next.exists),\n            eventCount: Math.max(0, Number(next.event_count || 0)),\n          };\n        }\n        return {\n          task: normalize(source.task),\n          run: normalize(source.run),\n          thread: normalize(source.thread),\n        };\n      }\n\n      function renderTeamTraceEvidence(task) {\n        const traces = teamTraceFiles(task);\n        const rows = [\n          { label: \"Task Trace\", value: traces.task },\n          { label: \"Run Trace\", value: traces.run },\n          { label: \"Thread Trace\", value: traces.thread },\n        ].filter((row) => row.value.path || row.value.exists);\n        if (!rows.length) {\n          return '<div class=\"empty\">Trace files will appear after dispatch and worker evidence are recorded.</div>';\n        }\n        return '<div class=\"team-id-list\">' +\n          rows.map((row) => {\n            const detail = row.value.exists\n              ? (String(row.value.eventCount) + ' events')\n              : 'pending';\n            const pathText = row.value.path ? short(row.value.path, 64) : '-';\n            return '<div class=\"team-id-row\">' +\n              '<span class=\"team-id-label\">' + esc(row.label) + '</span>' +\n              '<span class=\"team-id-value\" title=\"' + esc(row.value.path || \"\") + '\">' + esc(detail + ' · ' + pathText) + '</span>' +\n            '</div>';\n          }).join(\"\") +\n        '</div>';\n      }\n\n      function renderTeamDrawerActions(task, dashboard) {\n        const status = String((task && task.status) || \"queued\");\n        const owner = String((task && task.owner) || \"\");\n        const taskId = String((task && task.task_id) || \"\");\n        const runtime = task && task.runtime && typeof task.runtime === \"object\" ? task.runtime : {};\n        const traces = teamTraceFiles(task);\n        const ownerCanOpen = Boolean(owner && !owner.startsWith(\"pending-team-worker-\"));\n        const originalPrompt = teamTaskInputValue(task, \"prompt\");\n        function renderActionGroup(label, buttons) {\n          const actions = buttons.filter(Boolean);\n          if (!actions.length) return \"\";\n          return '<div class=\"team-drawer-action-group\">' +\n            '<div class=\"team-drawer-action-label\">' + esc(label) + '</div>' +\n            '<div class=\"team-drawer-action-buttons\">' + actions.join(\"\") + '</div>' +\n          '</div>';\n        }\n        return [\n          renderActionGroup(\"Navigate\", [\n            (ownerCanOpen ? '<button class=\"action-btn secondary\" data-focus-thread=\"' + esc(owner) + '\" type=\"button\">Select Thread</button>' : ''),\n            (ownerCanOpen ? '<button class=\"action-btn secondary\" data-codex-thread=\"' + esc(owner) + '\" type=\"button\">Codex</button>' : ''),\n            (ownerCanOpen ? '<button class=\"action-btn secondary\" data-open-codex-editor=\"' + esc(owner) + '\" type=\"button\">Editor</button>' : ''),\n          ]),\n          renderActionGroup(\"Inspect\", [\n            (runtime.log_path ? '<button class=\"action-btn secondary\" data-team-drawer-open-file=\"' + esc(runtime.log_path) + '\" data-team-drawer-open-file-label=\"Opened run log\" type=\"button\">Open Run Log</button>' : ''),\n            (traces.task.exists ? '<button class=\"action-btn secondary\" data-team-drawer-open-file=\"' + esc(traces.task.path) + '\" data-team-drawer-open-file-label=\"Opened task trace\" type=\"button\">Open Task Trace</button>' : ''),\n            (traces.run.exists ? '<button class=\"action-btn secondary\" data-team-drawer-open-file=\"' + esc(traces.run.path) + '\" data-team-drawer-open-file-label=\"Opened run trace\" type=\"button\">Open Run Trace</button>' : ''),\n            (traces.thread.exists ? '<button class=\"action-btn secondary\" data-team-drawer-open-file=\"' + esc(traces.thread.path) + '\" data-team-drawer-open-file-label=\"Opened thread trace\" type=\"button\">Open Thread Trace</button>' : ''),\n          ]),\n          renderActionGroup(\"Retry / Recovery\", [\n            ((ownerCanOpen && (status === \"failed\" || status === \"blocked\")) ? '<button class=\"action-btn warn\" data-team-drawer-retry-task=\"' + esc(taskId) + '\" data-team-drawer-retry-mode=\"same\" type=\"button\">Retry Same Thread</button>' : ''),\n            ((status === \"failed\" || status === \"blocked\") ? '<button class=\"action-btn secondary\" data-team-drawer-retry-task=\"' + esc(taskId) + '\" data-team-drawer-retry-mode=\"new\" type=\"button\">Retry in New Worker</button>' : ''),\n            (status !== \"completed\" ? '<button class=\"action-btn warn\" data-team-drawer-restart-task=\"' + esc(taskId) + '\" type=\"button\">' + esc(teamRunActionLabel(status)) + '</button>' : ''),\n            '<button class=\"action-btn secondary\" data-team-drawer-delete-task=\"' + esc(taskId) + '\" type=\"button\">Delete Task</button>',\n            (runtime.last_error ? '<button class=\"action-btn secondary\" data-team-drawer-copy-text=\"' + esc((runtime.last_error || \"\") + \"\\n\\n\" + originalPrompt) + '\" data-team-drawer-copy-label=\"Failure prompt + original task prompt\" type=\"button\">Copy Failure + Prompt</button>' : ''),\n          ]),\n        ].filter(Boolean).join(\"\");\n      }\n\n      function renderTeamTaskDrawer(payload, task) {\n        const drawer = document.getElementById(\"threadDrawer\");\n        const backdrop = document.getElementById(\"drawerBackdrop\");\n        const title = document.getElementById(\"drawerTitle\");\n        const meta = document.getElementById(\"drawerMeta\");\n        const summaryNode = document.getElementById(\"drawerSummary\");\n        const actionsNode = document.getElementById(\"drawerActions\");\n        const body = document.getElementById(\"drawerBody\");\n        if (!(drawer && backdrop && title && meta && summaryNode && actionsNode && body)) return false;\n        const dashboard = (payload && payload.dashboard) || { threads: [] };\n        const taskId = String((task && task.task_id) || \"\");\n        const owner = String((task && task.owner) || \"\");\n        const status = String((task && task.status) || \"queued\");\n        const runtime = task && task.runtime && typeof task.runtime === \"object\" ? task.runtime : {};\n        const retryState = describeTeamRetryState(runtime);\n        const progress = teamTaskProgress(task);\n        const originalPrompt = teamTaskInputValue(task, \"prompt\");\n        const compiledPrompt = teamTaskInputValue(task, \"compiled_prompt\");\n        const resultText = task && task.result ? JSON.stringify(task.result, null, 2) : \"\";\n        const taskJson = JSON.stringify(task || {}, null, 2);\n        const logs = payload && payload.teamCoordination && payload.teamCoordination.taskLogs && taskId\n          ? payload.teamCoordination.taskLogs[taskId] || []\n          : [];\n        drawer.classList.add(\"open\");\n        backdrop.classList.add(\"open\");\n        title.textContent = (task && task.title) || \"Team task\";\n        meta.innerHTML = [\n          '<span class=\"meta-pill mono\">Task ' + esc(short(taskId, 22)) + '</span>',\n          '<span class=\"meta-pill\">' + esc(status.toUpperCase()) + '</span>',\n          '<span class=\"meta-pill\">Owner ' + esc(teamThreadLabel(owner, dashboard)) + '</span>',\n          owner.startsWith(\"pending-team-worker-\")\n            ? '<span class=\"meta-pill\">Thread resolving</span>'\n            : (owner ? '<span class=\"meta-pill mono\" title=\"' + esc(owner) + '\">Thread ' + esc(short(owner, 30)) + '</span>' : ''),\n          runtime.pid ? '<span class=\"meta-pill mono\">PID ' + esc(String(runtime.pid)) + '</span>' : '',\n          runtime.command_kind ? '<span class=\"meta-pill\">' + esc(String(runtime.command_kind)) + '</span>' : '',\n          runtime.pid_running !== undefined ? '<span class=\"meta-pill\">Process ' + esc(runtime.pid_running ? \"alive\" : \"exited\") + '</span>' : ''\n        ].filter(Boolean).join(\"\");\n        summaryNode.innerHTML = [\n          drawerStat(\"Progress\", String(progress.percent) + \"%\"),\n          drawerStat(\"Dispatch\", runtime.state || status),\n          drawerStat(\"Retry\", retryState.summary || \"-\"),\n          drawerStat(\"Model\", runtime.model || (runtime.model_explicit === false ? \"CLI default model\" : \"-\")),\n          drawerStat(\"Task Events\", String(logs.length || 0)),\n          drawerStat(\"Lease\", task && task.lease_until ? formatTimestamp(task.lease_until) : \"-\"),\n          drawerStat(\"Updated\", task && task.updated_at ? formatTimestamp(task.updated_at) : \"-\")\n        ].join(\"\");\n        actionsNode.className = \"action-rail team-drawer-action-rail\";\n        actionsNode.innerHTML = renderTeamDrawerActions(task, dashboard);\n        body.innerHTML =\n          '<div class=\"drawer-section\">' +\n            '<h4 class=\"section-heading\">Goal</h4>' +\n            '<div class=\"kv-value\">' + esc((task && task.goal) || \"No goal recorded.\") + '</div>' +\n          '</div>' +\n          '<div class=\"drawer-section\">' +\n            '<h4 class=\"section-heading\">Identifiers</h4>' +\n            renderTeamIdentifierBlock(task) +\n          '</div>' +\n          '<div class=\"drawer-section\">' +\n            '<h4 class=\"section-heading\">Trace</h4>' +\n            renderTeamTraceEvidence(task) +\n          '</div>' +\n          '<div class=\"drawer-section\">' +\n            '<h4 class=\"section-heading\">Timeline</h4>' +\n            renderTeamTaskTimeline(task, logs) +\n          '</div>' +\n          '<div class=\"drawer-section\">' +\n            '<h4 class=\"section-heading\">Recent Event Feed</h4>' +\n            renderTeamMiniLogs(logs, \"No task events or inbox messages yet.\") +\n          '</div>' +\n          '<div class=\"drawer-section\">' +\n            '<h4 class=\"section-heading\">Original Prompt</h4>' +\n            '<pre class=\"team-drawer-pre\">' + esc(originalPrompt || \"-\") + '</pre>' +\n          '</div>' +\n          '<div class=\"drawer-section\">' +\n            '<h4 class=\"section-heading\">Compiled Prompt</h4>' +\n            '<pre class=\"team-drawer-pre tall\">' + esc(compiledPrompt || \"-\") + '</pre>' +\n";
