# Evolution Note — Spotlight Memory Shortcuts

## Plan

- path: `.claude/plans/ACTIVE_PLAN.md`
- milestone: `Milestone 3 — Insight and Guidance Layer (complete)`
- bounded target: add one minimal workflow-integration slice by surfacing Prompt / Rule / Memo quick-open shortcuts inside the selected-agent spotlight

## Completed

- Added a compact memory shortcut row to the selected-agent spotlight
- Reused existing repo-file quick-open behavior so Prompt, Rule, and Memo sources are now reachable from the active-agent surface rather than only from the separate memory panel
- Kept the slice limited to proximity and did not add editing or richer cross-card semantics

## Failed or Deferred

- Deferred deeper memory-agent linkage, editing flows, and richer memory behavior

## Decisions

- Treat spotlight-level memory shortcuts as the first bounded Task 3 step needed to bridge active agent context and memory artifacts
- Move next to a Task 3 review instead of adding more memory behavior in the same pass

## Next Handoff

```text
Continue the codex-loop from `.claude/plans/ACTIVE_PLAN.md`. Stay on `task-plans/prompt-rule-memo-cards-task-plan.md` and do one Task 3 review focused on whether the new spotlight-level memory shortcuts are enough to close the first workflow-integration gap.
```
